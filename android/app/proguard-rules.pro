# Default ProGuard rules. The app keeps no reflection-based code, so the
# library-provided rules (Room, Compose, etc.) are sufficient. Add app-specific
# keep rules here if you introduce serialization or reflection later.
