package no.visible.scrollreminder.ui.theme

import androidx.compose.ui.graphics.Color

// Visible brand palette
val BrandBlack = Color(0xFF111111)
val BrandOrange = Color(0xFFFF6600)
val BrandWhite = Color(0xFFFFFFFF)

// Supporting greys, derived from the black so the UI has depth without new hues.
val SurfaceDark = Color(0xFF1C1C1C)
val SurfaceElevated = Color(0xFF262626)
val OnSurfaceMuted = Color(0xFFB3B3B3)
val OutlineSubtle = Color(0xFF3A3A3A)
